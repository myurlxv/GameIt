// Enums.cs 파일

namespace Ninez.Stage
{
    public enum CellType
    {
        BASIC,
        EMPTY,
        // 다른 cell 유형이 필요한 경우 추가
    }

    public enum BlockType
    {
        BASIC,
        // 다른 block 유형이 필요한 경우 추가
    }
}
